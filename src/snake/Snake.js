import React, { Component } from 'react';

const Left, Up = -1;
const Right, Down = 1;

class Snake extends Component {
    constructor(props) {
        super(props)
        this.state = {
        body: [],
        head: {
                x: 0,
                y: 0,
            }
        }
    }
    move(direction) {
        switch(direction) {
            case 'left': {
                
            }
        }
    }
       
}